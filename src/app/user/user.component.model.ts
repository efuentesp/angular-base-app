import { Authority } from "./authorities.component.model";

export class User {
    //idUser : Number;
    username: string;   // <--- Only Login
    password: string;
    //rol: string;
    //token: string;
    //authorities: Authority;
}