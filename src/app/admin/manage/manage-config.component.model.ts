import { ConfigAuthority } from "./manage-authority.component.model";

export class AdminPermiso {

	private idGrupo: number = null;
	private nombreGrupo: string = '';
	private idPrivilege: number = null;
	private nombrePrivilege: string = '';
    private activeUser: number = null;
    private configAuthority: ConfigAuthority[];
 
}